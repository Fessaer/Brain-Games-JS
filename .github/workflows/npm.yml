name: lint

on:
  push:
      branches: [ master ]

jobs:
  eslint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
        run: npm install --save eslint-config-airbnb
      - name: Run linter
        run: make lint
